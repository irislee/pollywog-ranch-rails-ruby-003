<h1><%= @pond.name %> Pond</h1>
<p>water type: <%= @pond.water_type %></p>
<p>Resident frogs and their tadpoles:</p>
<ol>
  <% @pond.frogs.each do |frog| %>
    <li><%= link_to "#{frog.name}", frog %>
      <ol>
        <% frog.tadpoles.each do |tadpole| %>
          <li><a href="/tadpoles/<%= tadpole.id %>"><%= link_to "#{tadpole.name}" %> tadpole</a></li>
        <% end %>
      </ol>
    </li>
  <% end %>
</ol>
<%= link_to 'Back', ponds_path %>
<!-- <a href="/ponds">Back</a> --> | 
<%= link_to 'Edit', edit_pond_path %> | 
<%= link_to 'Destroy', @pond, :method=>:delete %>