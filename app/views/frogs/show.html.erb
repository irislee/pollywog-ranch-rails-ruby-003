<h1><%= @frog.name %> Frog</h1>
<p>Color: <%= @frog.color %></p>
<p>Lives: <%= link_to "#{@frog.pond.name}", pond_path %></p>
<% unless @frog.tadpoles.empty? %>
  <h2>Children</h2>
  <ol>
    <% @frog.tadpoles.each do |tadpole| %>
      <li><%= link_to "#{tadpole.name}", tadpole %></li>
    <% end %>
  </ol><br>
<% end %>
<p><strong><%= link_to 'Create new tadpole baby', "/frogs/" + @frog.id.to_s + "/tadpoles/new", :controller => "tadpoles", :action => "new"%></strong></p>

<%= link_to 'Back', frogs_path %> | 
<%= link_to 'Edit', edit_frog_path %> | 
<%= link_to 'Destroy', @frog, :method=>:delete %>

